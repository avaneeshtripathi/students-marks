require('../stylesheet/style.sass');
import React from 'react';
import ReactDOM from 'react-dom';
import Layout from './container';

const app = document.getElementById('app');
ReactDOM.render(<Layout/>, app);
